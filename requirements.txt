# Core runtime dependencies
# Install with: pip install -e .
#
# Optional extras:
#   pip install -e ".[gmail]"        — Gmail email provider + Drive upload
#   pip install -e ".[drive]"        — Google Drive upload only
#   pip install -e ".[microsoft365]" — Microsoft 365 email provider
#   pip install -e ".[pdf]"          — PDF report generation (weasyprint)
#   pip install -e ".[oracle]"       — Oracle database connections (oracledb)
#   pip install -e ".[all]"          — All of the above
#   pip install -e ".[dev]"          — Development tools (pytest, ruff)

# Web framework
flask==3.1.3
flask-sqlalchemy==3.1.1

# Database
psycopg2-binary==2.9.12
sqlalchemy==2.0.49
alembic==1.18.4

# Scheduling
apscheduler==3.11.2

# Templating
jinja2==3.1.6

# Reports
openpyxl==3.1.5

# CLI
click==8.4.0

# Encryption
cryptography==48.0.0

# Auth
PyJWT==2.12.1
bcrypt==5.0.0
pyotp==2.9.0

# CORS
flask-cors==6.0.2

# Rate limiting
flask-limiter==4.1.1

# Env
python-dotenv==1.2.2

# Task Queue
celery[redis]==5.4.0

# YAML export/import
pyyaml==6.0.3

# WSGI server + async worker (used by Docker / production deployments)
gunicorn==26.0.0
gevent==26.5.0

# ── Optional — install only what you need ────────────────────────────────────

# [gmail] + [drive] — pip install -e ".[gmail]"
# google-api-python-client==2.196.0
# google-auth==2.53.0
# google-auth-oauthlib==1.4.0

# [microsoft365] — pip install -e ".[microsoft365]"
# msal==1.36.0
# requests==2.34.2

# [mssql] — pip install -e ".[mssql]"   (also covers generic ODBC connections)
# pyodbc==5.2.0

# [pdf] — pip install -e ".[pdf]"
# weasyprint>=62.0

# [oracle] — pip install -e ".[oracle]"
# oracledb>=2.0
