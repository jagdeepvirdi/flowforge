1. make schedule (cron expression) easy to set. help(?) overlaps with top menu. Lets rethink how user sets cron expression. 
2. How can I pass parameters to report script or to db query, or db procedure? do we have logic to do that?
3. If I want to run a query Script by passing dates, for example a report that selects all the subsciptions created on in a week. How can I pass the week dates (Start and enddate) to subscription table so that only one week subscribers are selected. 
4. Keeping the same logic, what if I want to show the count of subscription in email and attach the report generated.
5. Also noted that when generating report the columns are shown as col0, col1, col2 ... and etc.
6. I dont the Microsoft onedrive in frontend UI system.
7. I had written a script sometime back and uploaded at https://github.com/jagdeepvirdi/DayToDayOfficeOperations.git The process is called Dataloader. which is a sqlloader process from oracle. Reason to build this was I wanted a process that could take file from multiple location to load into a Table, or multiple tables. and keep track of all the information. Like Files loaded, total number of data loaded successfully. data failed and etc. review the data in github location. check if we can incoperate it in our flowforge system.
8. when we run reports, some reports run everyday, some every week, some are monthly reports, or quarterly reports. How we can control the parameters passed to reports and view them on run history to validate the date time passed to the report query.
9. Does refresh button on top work?
10. Need summary and option to see more detailed help based on which window we are.