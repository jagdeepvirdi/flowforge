coverage:
  status:
    project:
      default:
        target: 70%
        threshold: 2%
    patch:
      default:
        target: 60%
        threshold: 5%

ignore:
  - "tests/**"
  - "docs/**"
  - "frontend/**"
  - "flowforge/db/migrations/**"
